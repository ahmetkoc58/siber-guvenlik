🔹 NAT Nedir?
NAT, ağ adresi çevirisi anlamına gelir.
Yerel ağdaki cihazların özel (private) IP adreslerini, internete çıkarken genel (public) IP adresine çevirir.
Böylece birden fazla cihaz tek bir genel IP üzerinden internete çıkabilir.



📌 NAT’ın Çalışma Mantığı
Yerel ağdaki bir cihaz (ör. 192.168.1.10) internete çıkmak ister.

NAT cihazı (genellikle router), bu özel IP’yi kendi genel IP’si ile değiştirir.

İnternetten cevap geldiğinde NAT cihazı, hangi özel IP’ye ait olduğunu bilir ve veriyi doğru cihaza yönlendirir.



📌 NAT Türleri
Static NAT

Bir özel IP → Bir genel IP eşlemesi sabittir.

Genellikle sunucular için.


Dynamic NAT

Özel IP’ler için genel IP havuzundan dinamik atama yapılır.

PAT (Port Address Translation) / NAT Overload

En yaygın kullanılan yöntem.

Birden fazla özel IP, aynı genel IP’yi kullanır, port numaralarıyla ayırt edilir.


📌 Siber Güvenlikte Önemi
NAT, iç IP’leri gizleyerek temel bir güvenlik katmanı sağlar.

Ama bu bir güvenlik duvarı (firewall) değildir — NAT cihazı saldırılara karşı tek başına koruma sağlamaz.

Bazı saldırılar (ör. NAT Traversal) NAT arkasındaki cihazlara erişmeyi hedefler.


💡 Benzetme:

NAT, apartmanın dış kapı numarası gibi düşün.

İçeride her dairenin kendi numarası var (private IP), ama dışarıdan bakıldığında tek bir adres görünüyor (public IP).

