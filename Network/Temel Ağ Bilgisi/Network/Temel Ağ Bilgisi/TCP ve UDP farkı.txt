🔹 TCP (Transmission Control Protocol)
Özellikleri:

Bağlantı tabanlıdır (connection-oriented). Veri gönderilmeden önce iki taraf arasında bağlantı kurulması gerekir.

Güvenilir veri iletimi sağlar:

Paketlerin sırası korunur.

Eksik paketler tekrar gönderilir.

Veri bozulursa yeniden istenir.

Yavaş ama güvenilir: Hata kontrolü ve onay (ACK) mekanizması olduğu için hız biraz düşer.


Kullanıldığı yerler:

Web (HTTP, HTTPS)

E-posta (SMTP, IMAP, POP3)

Dosya transferi (FTP)


📌 Siber güvenlikte önemi:

TCP 3-Way Handshake aşaması (SYN, SYN-ACK, ACK) saldırılarda hedef olabilir (SYN Flood).

TCP bağlantısı, IDS/IPS sistemlerinde kolayca takip edilebilir.




🔹 UDP (User Datagram Protocol)
Özellikleri:

Bağlantısızdır (connectionless). Veri göndermeden önce bağlantı kurulmaz.

Güvenilirlik garantisi yoktur:

Paketler kaybolabilir veya sırası değişebilir.

Yeniden gönderim yoktur.

Hızlı ama güvenilir değil: Hata kontrol mekanizması olmadığı için düşük gecikme sağlar.


Kullanıldığı yerler:

Canlı yayın, VoIP (sesli görüşme)

Online oyunlar

DNS sorguları


📌 Siber güvenlikte önemi:

UDP, hızlı olduğu için DDoS saldırılarında sıkça kullanılır (ör. DNS Amplification).

Trafiğin bağlantısız olması, paket takibini zorlaştırabilir.


🔹 Kısaca Fark
TCP: Güvenilir, yavaş → veri bütünlüğü önemliyse tercih edilir.

UDP: Hızlı, güvensiz → hız önemli, paket kaybı tolere edilebiliyorsa tercih edilir.


💡 Benzetme:

TCP → Kargo şirketi: Her paket teslim edilip edilmediğini takip eder.

UDP → Mektup atmak: Gidip gitmediğini kontrol etmezsin.